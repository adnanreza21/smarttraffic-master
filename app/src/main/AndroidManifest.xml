<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aplikasiku.navwithdirectionlib">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_withoutname"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBPWWTZcnHwIfe8c_gLJkaLm8WGdJnLS7Y" />

        <activity android:name=".hal_home" />
        <activity android:name=".hal_map" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".HelloFirebaseDB" />
        <activity android:name=".KontrolerLampuActivity" />
        <activity android:name=".SkemaTLbaruActivity" />
        <activity android:name=".AdminActivity" />

        <service android:name=".GPS.GoogleService" />

        <activity android:name=".InserterHospitalActivity" />
        <activity android:name=".InsertRsFirebase" />
        <activity android:name=".SetDestinationActivity"></activity>
    </application>

</manifest>